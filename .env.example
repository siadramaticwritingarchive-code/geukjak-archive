# Copy this file to .env.local and fill in values from Supabase Project Settings.
# Only use the public anon key in the browser. Never expose the service role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
